import React from 'react'
import PropTypes from 'prop-types'
import styles from './styles/HBackground.module.css'

const HBackground = (wave, shapeFill) => {
  return (
    <div className={styles.wave}>
      <p>
        
      </p>
   </div>
  )
}

export default HBackground